# Change Log - QA Test Case Architect v2

Use this page to track changes to the agent design, knowledge sources, Studio configuration, evaluations, measurement, and governance status.

| Version | Date | Area | Change Summary | Reason / Outcome | Updated By |
|---|---|---|---|---|---|
| v0.1 | 2026-06-11 | Initial governed documentation scaffold | Created Project Brain, Agent Design Record, Knowledge Source Plan, Subagent Settings, Studio configuration copy blocks, Evaluation, and Measurement Plan. | Convert the agent self-description report into reviewable Confluence source drafts without approving launch or Studio writes. | Codex documentation scaffold |
| v0.2 | 2026-06-11 | Copilot conversation summary additions | Added retrieval-generation-validation pipeline, Feature Brief output, validation gates, golden-copy example gaps, broad-source restrictions, and unresolved full artifact catalog questions. | Incorporate follow-up design context while keeping the agent Draft / Needs Cleanup and write actions disabled. | Codex documentation scaffold |
| v0.3 | 2026-06-11 | Studio setup model | Added a dedicated Studio Setup page as the human manual setup guide and reframed the ROVO Studio Configuration page as an agent-facing configuration source. | Keep Studio instructions lean while making knowledge source and skill scoping easier to copy, audit, and test. | Codex documentation scaffold |
| v0.4 | 2026-06-11 | Publish placement | Set the recommended publish location to `Agent Project Brains > QA and Testing Agents`. | Use the existing QA and Testing Agents Confluence parent page for the QA Test Case Architect v2 page family. | Codex documentation scaffold |
| v0.5 | 2026-06-11 | Evaluation remediation | Added virtual source-packet guidance after Studio evaluation showed the agent blocking on missing formal XML ticket-pack files despite complete prompt evidence. | Improve evaluation behavior so prompt-provided story evidence can be used as the temporary input package. | Codex documentation scaffold |
| v0.6 | 2026-06-11 | Production model clarification | Confirmed production requires `TICKET_PACK_COMBINED.xml`, documented the nine required artifacts, clarified manual Copilot/XRAY CSV flow, and added a sanitized golden-copy XRAY CSV fixture. | Align governance docs with the intended production workflow while preserving prompt packets for AgentLab and Studio evaluation only. | Codex documentation scaffold |
| v0.7 | 2026-06-11 | AgentLab-style simulation | Added local AgentLab-style simulation report, CSV column skeleton, continuation-row rule, artifact alias handling, masked banking-data examples, AgentLab/production gate starters, and full-package batching guidance. | Reduce evaluation prompt drift and strengthen Studio setup before live AgentLab testing. | Codex documentation scaffold |
| v0.8 | 2026-06-11 | Simulation pass 2 baseline | Reran the AgentLab-style simulation against the improved setup and recorded a 100 / 100 high-ROI simulated baseline across the six v2 evaluation prompts. | Establish that the current prompt/config is ready for live Studio setup testing, while keeping production ticket-pack fixture testing as the next iteration. | Codex documentation scaffold |
| v0.9 | 2026-06-11 | Simulation pass 3 production baseline | Added a synthetic `TICKET_PACK_COMBINED.xml` fixture and ran production-flow simulation cases for missing ticket pack, present ticket pack, full-package batching, conflict handling, and broad-source refusal. | Met the stop rule with two consecutive 90 percent or higher passes; no pass 4 or pass 5 required for the current baseline. | Codex documentation scaffold |
| v0.10 | 2026-06-11 | Robert review updates | Added default all-nine ArtifactPack behavior, single-artifact exception, high-risk AC categories, production conflict escalation, evaluation-only precedence rationale, and clarified that Jira exports or attachments do not replace the production ticket pack. | Align parent instructions with Robert's intended manual testing model before Studio configuration and dataset evaluation. | Codex documentation scaffold |
| v0.11 | 2026-06-15 | Category eval hardening | Hardened AC normalization against deduplication, required full deterministic test ID format, and added raw-only XRAY CSV output mode after Category 2, 3, and 4 each scored 80 percent. | Address the three observed unresolved cases before Robert's next review pass. | Codex documentation scaffold |
| v0.12 | 2026-06-15 | XRAY CSV continuation-row hardening | Added explicit override that governed continuation-row rules supersede conflicting retrieved template wording, required exact `Execution Type` / `Test Type` header, and added a pre-return CSV self-check. | Address the remaining Category 4 80 percent failure where metadata repeated on continuation rows and `Test Type` was duplicated. | Codex documentation scaffold |
| v0.12a | 2026-06-17 | Runtime contract split (live-only history, reconciled 2026-07-14) | Added `Runtime Contract - QA Test Case Architect v2` as the behavior authority, kept Studio Setup as the human manual setup page, and reframed ROVO Studio Configuration as the Studio-ready copy/configuration source. | Align the agent with the thin Studio setup and runtime contract modernization pattern without changing live Studio, Jira, XRAY, or Confluence. Captured directly from the live Confluence page (version 6, 2026-06-17), which had not been recorded as its own change-log row in the repo. Numbered `v0.12a` (rather than reusing `v0.13`) to avoid colliding with the already-assigned repo `v0.13` row below, which documents a different, later (2026-07-13) decision set. `v0.21` later re-applied/reconciled this Runtime Contract split into the repo source. | Live Confluence history (reconciled into repo 2026-07-14 by Claude Code) |
| v0.13 | 2026-07-13 | Session-1 follow-up: S2 decision + S6/S9/S10/S11 | Recorded the **S2 decision**: the XRAY CSV column contract is **shared across teams** (not team-specific), per QA leadership ruling relayed by the owner. Reaffirmed the earlier decision that `Priority` is dropped from the import contract. Applied S6 (clarification/intake gate, **ticket-attach-only** default confirmed by the owner) as a canonical parent-instruction change and S9 (Studio collaborate/attach permission doc) via companion PR #19; added S10 (CSV-to-file + import round-trip plan) and S11 (time-savings hypothesis, explicitly not ROI) as records via PR #18. | Unblock S3 while keeping the enforced XRAY column contract and TC-ID rule unchanged until the owner reviews the [contract-diff note](../reports/qa-test-case-architect-v2-csv-contract-diff-2026-07.md). **S3 held for owner review; S4 TC-ID convention held for a written owner convention.** The confirmed shared contract will reverse the v0.11/v0.12 duplicate-`Test Type` hardening when S3 is applied. No ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (governance workbench); decisions relayed by Shayne Vandersloot |
| v0.14 | 2026-07-13 | S3 XRAY contract reconcile (shared 14-column contract) | Reconciled the enforced XRAY CSV contract to the team-confirmed shared 14-column contract: two literal `Test Type` columns (first Manual/Automated, second functional category), added `Functionality`, removed `Priority` and `Sprint`, renamed `Assignee Name` to `Assignee`, recased `Application list`, and updated the continuation-row and self-check rules. Updated the ROVO Studio Configuration source, thin parent instructions, golden-copy XRAY template, Category 4 evaluation (promoted T-1), and the evaluation hardened-behavior note. | Apply the approved S2 shared-contract decision (full reconcile). Reverses the v0.11/v0.12 duplicate-`Test Type` hardening, which conflicted with the confirmed golden. Studio save remains a separate manual step; no ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.15 | 2026-07-13 | S4 configurable deterministic TC-ID + Payment Ninjas overlay | Made the deterministic test-case ID rule configurable per team overlay: base/default remains `TC-{StoryID}-{AC}-{AREA}-{TYPE}-{NNN}`; the Payment Ninjas overlay uses `{ProjectPrefix}-{StoryNumber}-{Seq}` (example sanitized 2026-07-16 to `SYNTH-1001-1`). Added the QATCA v2 Payment Ninjas team overlay page and eval QATCA-EVAL-019 (Session 1 T-4) covering pattern formatting, deterministic stable reruns, and base fallback. | Apply the approved S4 decision. IDs stay deterministic/stable; ROVO draft IDs are not final XRAY keys. Studio save remains a separate manual step; no ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.16 | 2026-07-13 | S8 optional readable table (non-raw-only) | Allowed a human-readable rendered table alongside the raw CSV in default (non-raw-only) responses, while keeping raw-CSV-only output exactly raw header + rows (readable table explicitly excluded from raw-only mode). Added eval QATCA-EVAL-020 (Session 1 T-7) covering both modes; existing Category 4 raw-only rows remain the regression guard. | Apply the approved S8 decision without polluting raw-only mode. No change to the S3 14-column contract or S4 TC-ID rule. Studio save remains a separate manual step; no ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.17 | 2026-07-14 | Re-add Priority column (default Low) | Re-added `Priority` to the enforced XRAY contract (now 15 columns), positioned after `Functionality`, default value `Low`. Updated ROVO Studio Configuration, thin parent instructions, golden-copy template, Category 4 eval (incl. T-1), and the evaluation hardened-behavior note; recorded the reversal in the contract-diff note. The committed team-golden import fixture is intentionally left 14-column (it predates the Priority requirement). | Apply the Session-2 decision (`qa-test-case-architect-v2-session-2-csv-import-decisions-2026-07.md`): import may succeed without Priority, but downstream workflow completion requires it; default Low, QA can raise later. Reverses the 2026-07-13 Priority-drop (v0.14/S3). Exact column index pending confirmation against a real Priority-bearing import. Studio save remains a separate manual step; no ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.18 | 2026-07-14 | S7 test-type + Application List allow-list validation | Added allow-list validation: the two `Test Type` columns are constrained to approved values (execution mode = Manual/Generic/Cucumber, team uses Manual; functional type from the 26-value Jira/XRAY list, captured in the Knowledge Source Plan), and `Application List` is constrained to the approved Jira/XRAY picklist referenced as a knowledge source (full internal list intentionally not committed). Behavior: infer -> show -> confirm -> refuse/flag out-of-list values -> draft-only when validation fails. Added eval QATCA-EVAL-021 (Session 1 T-6 / Session 2). | Apply the approved S7 decision using the Session-2 controlled vocabularies. Application List kept reference-only to avoid committing the internal application inventory. Open: cross-team scope, multi-value delimiter, list governance owner. Studio save remains a separate manual step; no ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.19 | 2026-07-14 | S5 step-quality/preconditions + App List answers | Added a test-step quality + precondition layer sourced from the SME-approved golden set (style/patterns not verbatim; repeated setup like login/claim-creation expressed as preconditions; missing setup logged in TBDLog). Added the team-overlay Step Style And Preconditions section and evals QATCA-EVAL-022/023 (Session 1 T-2/T-3). Recorded confirmed S7 Application-List answers: shared org-wide, single-value, owned by Jira/XRAY admins, list reference-only; flagged field-hygiene cleanup. | Apply the Session-2 decisions; unblocks S5 using the approved golden set. Studio save remains a separate manual step; no ROVO Studio/Jira/XRAY/Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.20 | 2026-07-14 | Downloadable CSV output (final validated step) | The agent now generates the finalized, validated TestSuite CSV as a downloadable UTF-8 `.csv` file for the human to import into XRAY, removing the manual Copilot conversion step. Raw-CSV-only mode still returns text; draft-only when validation fails; the agent still does not upload/import to XRAY/Jira/Confluence (the human imports). Added eval QATCA-EVAL-024; annotated the CSV-to-file plan. Depends on the runtime supporting file emission (falls back to text otherwise). | Apply the Session-2 decision (#4) to remove the Copilot middle-step. Preserves the write boundary and S7 draft-only behavior. Studio save remains a separate manual step; no ROVO Studio/Jira/XRAY/Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.21 | 2026-07-14 | Reconcile repo with live Runtime Contract structure | Created the repo `Runtime Contract - QA Test Case Architect v2` page (captured from the live 2026-06-17 baseline) and applied all merged behavior changes to it (15-column shared contract with dual `Test Type`, `Functionality`, `Priority` default Low, no `Execution Type`/`Sprint`; configurable TC-ID; ticket-attach intake gate; Test Type/Application List allow-list validation; readable table; downloadable CSV; step-quality/preconditions). Aligned the ROVO Studio Configuration and Studio Setup pages to name the Runtime Contract as the behavior authority. Added the manifest entry. | Reconcile the repo↔live drift before publishing (the live pages carried the OLD contract plus a Runtime Contract layer the repo lacked). No ROVO Studio/Jira/XRAY/Confluence writes; publish is a separate approved step. | Claude Code (Sonnet implementer); approved by owner |
| v0.22 | 2026-07-14 | S7 refinement: Test Type sourced from the Jira story field | Clarified that the functional Test Type value is sourced from the Jira story's test-type field (the story is the source of truth); the approved 26-value functional list is canonical validation data (the allowed set), not a value inferred from prose. Updated the Runtime Contract, ROVO Studio Configuration, and Studio Setup XRAY-CSV/Test-Type rules, the Knowledge Source Plan Controlled Vocabularies section, and resolved the Payment Ninjas overlay's deferred test-type item. Execution-mode Test Type stays Manual-default; Application List inference unchanged. | Owner decision: "whatever is attached in the test type field is the test type; use the list to choose from as canonical data, but the source is the Jira story." Studio save remains a separate manual step; no ROVO Studio/Jira/XRAY/Confluence writes. | Claude Code (Sonnet implementer); approved by owner |
| v0.23 | 2026-07-16 | Manual-only execution mode, evaluation-mode metadata, evaluation-stop hardening, synthetic ID examples | Recorded the 2026-07-16 owner decisions and applied them to the Runtime Contract, ROVO Studio Configuration, Studio Setup, Knowledge Source Plan (Controlled Vocabularies), Payment Ninjas overlay, Category 1 dataset, and Evaluation page: (1) the first `Test Type` (execution mode) column's default and only approved value is `Manual` — `Automated`, `Generic`, `Cucumber`, or other values are not emitted unless a future approved source changes the contract; the separate `Automated` column stays Yes/No. (2) Non-raw Studio evaluation/AgentLab responses begin with `Mode: Evaluation` and `Mode trigger:` lines; mode metadata never appears in TBDLog (retires the Category 1 `EvaluationMode=True`-in-TBDLog expectation); raw-CSV-only responses remain CSV-only, and mode classification for raw-only rows is verified through the harness. (3) Evaluation mode never bypasses missing-data stops, validation, conflict handling, privacy restrictions, source isolation, or safety rules; source isolation is now defined for evaluation runs (generate only from the supplied packet and configured fixtures; no live Jira/Confluence retrieval mixed into a synthetic packet unless the prompt explicitly requests it, with any retrieved sources listed). (4) Replaced the real-looking TC-ID example with the synthetic `SYNTH-1001-1` across current-truth pages and examples. Added evals QATCA-EVAL-027/028/029/030 (collision-checked; live set previously ended at 026). | Port of the 2026-07-16 approval set onto the reconciled v0.22 baseline, on a fresh branch from origin/main after the stale-worktree drift stop (a prior worktree based on a pre-v0.14 main had produced colliding v0.14–v0.16 entries and eval IDs; that work was not committed). Confluence republish and manual Studio save remain separate gated steps; no ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (governance workbench); decisions relayed by Shayne Vandersloot |
| v0.24 | 2026-07-16 | v0.23 evaluation results + Studio save verification (no runtime change) | Recorded the first post-v0.23 Studio grader run (Categories 1, 4, 6; 19 rows) and its independent review against the merged contract: grader scores 80% / 89% / 100% (from 40% / 50% / 80% baselines); independent strict verdicts 14/19 clean, 2 Fail, 3 Partial. QATCA-EVAL-027/028/029 marked **Pass** on the Evaluation page (unambiguously exercised); EVAL-030 not exercised; EVAL-031 (source isolation) not yet authored, though no live-source mixing was observed in any row. Run provenance established by the owner's live parent-instruction pull, verified content-identical to the approved v0.23 copy block (instruction capture #2). Findings register R-1–R-8 opened in the [results review](../reports/qa-test-case-architect-v2-studio-evaluation-v0.23-results-2026-07-16.md): headline items are R-1 (production pasted-export bypass recurs — deferred F2, High), R-4 (CSV field-integrity defects the grader and self-check both miss), and R-2/R-5 (deferred F9/F10 still open). | Keep the audit trail truthful about the first attributable v0.23 run without claiming readiness: score improvement maps exactly to what v0.23 changed, and residual failures map exactly to what v0.23 deferred. No runtime instruction, dataset, or live system was changed by this entry's slice; raw grader exports stay out of Git (sanitized report only). | Claude Code (governance workbench); evidence supplied by owner |
| v0.25 | 2026-07-16 | F2/R-1 production-mode enforcement + R-4 CSV structural validation (draft; Studio save pending) | Remediates the two High findings from the v0.24 evaluation evidence: (1) mode-precedence self-check — a request is Evaluation only when explicitly marked Studio evaluation/AgentLab/prototype; production-labeled or unmarked requests follow production rules even with a pasted packet, stop without the ticket pack, generate no artifacts, and never open with `Mode: Evaluation`; (2) CSV structural validation — every emitted row must parse to exactly 15 fields, comma/quote/newline-bearing fields are double-quoted, continuation rows carry exactly 13 leading blanks with Action populated whenever Expected Result is, and the Story ID repeats on each test case's first row. Applied to the Runtime Contract, ROVO Studio Configuration, and Studio Setup copy blocks; added Category 1 and Category 4 regression rows and evals QATCA-EVAL-032/033 (031 stays reserved for source isolation); reconciled the tracked Category 4 dataset with the 9 rows actually graded in the v0.24 evidence run (ported priority-default C4-006 and downloadable C4-007/C4-008, which existed only in the frozen pre-v0.23 worktree); R-4 treated as High per owner 2026-07-16 (import-breaking and grader-invisible). | Remediate results-review findings R-1 and R-4 (docs/reports/qa-test-case-architect-v2-studio-evaluation-v0.23-results-2026-07-16.md). Confluence republish and manual Studio save remain separate gated steps; no ROVO Studio / Jira / XRAY / Confluence writes in this slice. | Claude Code (governance workbench); sequencing approved by owner |
| v0.26 | 2026-07-21 | v0.25 evaluation results: R-1 closed, R-4 open (no runtime change) | Recorded the post-v0.25 Studio grader run (Category 1 Redux-2: 6/6; Category 4 Redux-2: 10/11) and its independent review: **closure criterion 1 met — R-1/F2 closed** (all four production rows, including the QATCA-EVAL-032 trap, stopped without artifacts or mode lines; EVAL-032 marked Pass); **closure criterion 2 not met — R-4 stays open** (dedicated regressions C4-009/010 pass with full quoting and 13-blank continuation rows, EVAL-033 marked Partial, but three organic rows still violate field counts: two unquoted-comma 16-field rows and one 14-field metadata row). Also recorded: R-3 fixed-on-evidence (CoverageMap emitted as CSV; the grader's contrary verdict misread the TBDLog table); R-6 improved (no fabricated file link; honest text fallback); **F10 escalated** — a real name plus full Atlassian ARI people-link appeared inside the Assignee field of raw importable CSV. Root causes for the R-4 iteration: quoting applied only when prompts mention commas, and the v0.25 rule's "exactly 15 fields" wording is TestSuite-shaped (CoverageMap/RTM need per-artifact column-count wording). | Record results without changing baselines or claiming readiness, per the owner's instruction. Provenance is behavioral-plus-owner-statement; parent-instruction capture #3 remains pending. Recommended next: capture #3, R-4 iteration 2 (universal quoting + per-artifact column counts), F10 promoted ahead of or alongside F9 (owner decision), then F9 and EVAL-031. | Claude Code (governance workbench); evidence supplied by owner |
| v0.27 | 2026-07-21 | Owner status decision: Controlled QA checkpoint (no runtime change) | Recorded the owner's checkpoint decision following the v0.26 results: **Controlled QA checkpoint — production gate closed (R-1/F2), CSV import readiness (R-4) and assignee privacy (F10) remain open.** Usage restrictions until further notice: continued testing with **synthetic data only**, **no live XRAY imports** (malformed CSV can still reach outputs), no broad team rollout, and no readiness claim. Remediation order reset by the owner: (1) R-4 iteration 2 (universal quoting; per-artifact column counts; Expected-Result-required check), (2) **F10 assignee sanitization/redaction — promoted ahead of F9** (real names and Atlassian ARIs in importable output are a privacy issue), (3) capture #3 (saved Studio parent-instruction pull) plus regression reruns, (4) F9 and EVAL-031 afterward. New scope is paused until these complete. | Keep the audit trail truthful about the agent's usage posture at this checkpoint. This is a pause-and-restrict decision, not a promotion, readiness, or retirement action; lifecycle status remains Draft / Needs Cleanup. No runtime instruction, dataset, or live system changed by this entry. | Owner decision (Shayne Vandersloot); recorded by Claude Code |
| v0.28 | 2026-08-25 | New companion agent hand-off: QA Test Adequacy Critic (routing addition only) | Added a new governed agent candidate, QA Test Adequacy Critic, from the owner's P1 priority-list item and supplied instruction file (see `Change Log - QA Test Adequacy Critic`, v0.1). Added one Routing Rules row and a Subagent Settings note on this agent: after generating a complete production TestSuite/ArtifactPack (not evaluation mode, not a single-artifact request, not partial output), end the response with a recommendation to run the output through QA Test Adequacy Critic before treating it as review-ready. Hand-off is human-mediated pending confirmation of any Studio agent-to-agent invocation capability — no hidden invocation is claimed. No other QATCA v2 behavior changed in this entry; the ticket-pack removal, search-scope, coverage, and XRAY-schema changes from the same priority-list round are tracked separately and land in a later slice. | Apply the P1 "Add ISTQB Coverage Critic Sub-Agent" decision without bundling it into the larger P2 intake-model/schema rewrite, keeping this slice small and reviewable. No ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (governance workbench); source instructions and priority decisions supplied by the owner |
| v0.29 | 2026-08-25 | Retire Ticket Pack Builder + bounded search scope + team-overlay discovery + XRAY schema correction + governance alignment (P2 bundle) | Bundled the remaining P2 items from the same 2026-08-24/25 priority-list round: **(1) Ticket-pack removal and search scope** — retired the `TICKET_PACK_COMBINED.xml` / Ticket Pack Builder production requirement; minimum required input is now at least one Jira epic or story link, from which the agent gathers its own context (description/ACs/comments/history/attachments on the epic and story; issue links; a search bounded to the home project plus one hop into an explicitly linked project, plus Confluence, using epic rollup as the relatedness signal; and team-overlay discovery by project prefix). Applied to Runtime Contract (new Source Context Gathering section replacing Production Ticket-Pack Rule; updated Source Discipline table and Runtime Verification), ROVO Studio Configuration, Studio Setup, Knowledge Source Plan, and Project Brain. **(2) XRAY schema correction (§10, reverses v0.17)** — removed the `Priority` column (15→14 columns); simplified the deterministic ID fallback from the verbose `TC-{StoryID}-{AC}-{AREA}-{TYPE}-{NNN}` to the sequence-based `{StoryID}-{Seq}` (example `BB26-1197-1`); continuation-row leading-blank count corrected 13→12. Applied to Runtime Contract, ROVO Studio Configuration, Studio Setup, the Payment Ninjas overlay's base-fallback reference and column-contract line, the golden-copy XRAY CSV fixture, and the Category 4 evaluation dataset (11 rows). **(3) Governance alignment (§4)** — recorded that both usage modes are intentionally valid, not one superseding the other: evaluation and AgentLab runs use synthetic fixtures only; approved-team production runs (Payment Ninjas/PN, Breaking Backlogs/BB26, and 404 Errors/FOURFOUR26 — named-team scoping, not a general production model) use real Jira data and real XRAY import. This supersedes the v0.27 checkpoint's "synthetic data only, no live XRAY imports" wording, which had become a standing contradiction with both current practice and this direction. Added Evaluation page eval cases QATCA-EVAL-031 (source isolation, previously reserved), 034 (bounded search scope), 035 (team overlay discovery), and 036 (ticket-pack retirement); updated EVAL-011, 019, 020, and 033 for the corrected numbers. | Apply the owner-approved §3+§10+§4 bundle together, since both the intake-model rewrite and the schema correction touch the same CSV/context-gathering sections (matches this repo's v0.25 precedent of bundling F2+R-4). Governance alignment lands in the same pass so the search-first, real-data-gathering model is not published under a record that still says synthetic-only. No ROVO Studio / Jira / XRAY / Confluence writes. Known gap: the Category 1, 6, and 7 evaluation datasets and `qa-test-case-architect-v2-studio-evaluation-prompts-v2.csv` still reference the retired ticket pack in their scenario prompts and were not rewritten in this slice (tracked in Pending Change Gates below) — only Category 4 (directly tied to the schema correction) was updated. | Claude Code (governance workbench); sequencing and named-team scoping approved by owner |
| v0.30 | 2026-08-25 | Product/state coverage validation + coverage-completeness self-check (P1 bundle, §2 + §7.1) | Added the refined product/state coverage rule: when source evidence names specific products (e.g., HO3, SS/Signature Series, DP3), states/jurisdictions (e.g., AZ, NJ, CO), or a material variant of either, treat each distinct combination as its own required test case by default; clubbing multiple combinations into one generalized test is the rare, deliberately-noted exception, not the default. Products/states are detected from source evidence only — no canonical list is maintained (confirmed 2026-08-25; propose adopting a company-maintained list later as a governed enhancement if one is discovered, rather than folding it in silently). Added the general coverage-completeness self-check (§7.1, lands in the same instruction slice per the plan): before returning TestSuite output, review positive/negative/boundary/alternate-flow/error-handling coverage per AC and per product/state combination, logging any gap in TBDLog. Added Data Incomplete flag QATCA-DI-010 for a named combination with no identifiable test. Applied to the Runtime Contract (AC And Test Generation Rules, Runtime Verification), ROVO Studio Configuration, Studio Setup (Operating/Safety rules, two new smoke tests), and Knowledge Source Plan (Source Handling Rules, Data Incomplete Flags). Added Evaluation page eval cases QATCA-EVAL-037 (per-combination default), 038 (deliberate clubbing exception), 039 (coverage-completeness self-check), and 040 (uncovered combination logged). This directly feeds the QA Test Adequacy Critic's per-AC test-count and product/state coverage checks (its Review 1 and its own eval cases QTAC-EVAL-007/010) by raising QATCA v2's own generation baseline. | Apply the P1 "Product/State coverage validation" decision from the 2026-08-24/25 priority-list round, bundled with §7.1 per the plan's own sequencing (both land in the same AC/Test Generation Rules instruction slice). No ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (governance workbench); decisions relayed by the owner |
| v0.31 | 2026-08-25 | Team overlay CSV column confirmation before generating (P2, §5) | Added a confirm-before-generate rule: before generating TestSuite/XRAY CSV, the agent confirms with the user that the standard 14 columns are sufficient. If the user requests an additional column beyond the shared contract, the agent does not alter that run's output schema on the spot — it generates the standard 14-column contract, logs the requested addition in TBDLog as a proposed team-overlay change, and tells the user a new column requires a governed overlay decision, not an ad hoc addition. This preserves the S2/S3 decision that the XRAY column contract is shared across teams, not team-specific, rather than reversing it through agent behavior. Applied to the Runtime Contract (XRAY CSV Rules, Runtime Verification), ROVO Studio Configuration, and Studio Setup (Operating/Safety rules, two new smoke tests). Added Evaluation page eval cases QATCA-EVAL-041 (confirm-before-generate) and 042 (additional-column request handling). | Apply the owner-confirmed §5 recommended default ("no ad-hoc column additions; confirm the standard 14-column schema, log any requested addition as an enhancement requiring a governance decision") from the 2026-08-24/25 priority-list round. No ROVO Studio / Jira / XRAY / Confluence writes. | Claude Code (governance workbench); decision confirmed by the owner |

## Pending Change Gates

| Gate | Status | Notes |
|---|---|---|
| Owner assigned | Open | Shayne Vandersloot is a likely candidate from the report but not formally assigned. |
| Backup owner assigned | Open | Required before pilot. |
| QA standard template confirmed | Open | Confirm page location, version, and Studio knowledge source. |
| Ticket Pack Builder workflow confirmed | Closed (retired) | Retired 2026-08-25 (v0.29); production now requires only a Jira epic or story link, with the agent gathering its own bounded context. |
| Bounded search scope and team-overlay discovery evaluated | Open | New self-directed behavior (v0.29); needs live/synthetic evaluation evidence (QATCA-EVAL-034/035). |
| Category 1/6/7 and studio-evaluation-prompts-v2 datasets reconciled with ticket-pack retirement | Open | These datasets still phrase scenarios around `TICKET_PACK_COMBINED.xml`; only Category 4 was updated in v0.29 (directly tied to the schema correction). |
| Governance scope for real-team production usage recorded | Done (v0.29) | Named-team scoping (PN, BB26, FOURFOUR26); any other team requires its own onboarding decision before real Jira/XRAY use. |
| Product/state coverage and coverage-completeness self-check evaluated | Open | New rules (v0.30); needs live/synthetic evaluation evidence (QATCA-EVAL-037/038/039/040). |
| Confirm-before-generate column check evaluated | Open | New rule (v0.31); needs live/synthetic evaluation evidence (QATCA-EVAL-041/042). |
| Jira/XRAY integration model confirmed | Partial | Current design assumes ROVO returns CSV text, human copies it into Copilot or another manual step, and final CSV/import remains manual until an approved connector exists. |
| Full artifact catalog confirmed | Done | Required artifacts are FeatureBrief XML, TestSuite XRAY CSV, CoverageMap CSV, RTM CSV, E2E_Scenarios TXT, MiniTestPlan XML, TestSummary XML, ConflictLog TXT, and TBDLog TXT. |
| Golden-copy examples collected | Partial | Sanitized XRAY CSV golden-copy fixture exists; remaining artifact examples are still needed. |
| Validation thresholds defined | Open | Coverage, XRAY, CSV, and traceability pass/fail criteria need QA owner review. |
| Studio manually configured or captured | Open | No automated Studio write was performed during scaffolding. |
| Studio setup page published | Open | Required before Studio instructions can reference the live setup/source pages by URL. |
| Evaluation run | Partial | Local AgentLab-style pass 2 and pass 3 reached the high-ROI baseline in consecutive runs; live Studio/AgentLab run still needed after manual configuration. |
| Pilot review complete | Open | Needed before promotion. |
