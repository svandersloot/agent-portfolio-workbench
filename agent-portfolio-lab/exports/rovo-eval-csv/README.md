# Rovo Eval CSV Exports

Stage CSV-compatible manual evaluation rows.

Use the first-slice CSV shape:

```text
prompt,expected_result
```

Do not include live URLs, real project keys, account IDs, or private release data.
