# Sanitized Jira Issue Sets

Store sanitized Jira issue excerpts for manual routing, triage, and draft bundle tests.

Use placeholder issue keys such as `[Jira issue]` and project placeholders such as `[Jira project]`.

Do not include live Jira URLs, real issue keys, account IDs, assignees, reporter names, or private comments.
