# Sanitized Release Drift Drafts

Store sanitized Release Drift Monitor draft outputs and drift pulse examples.

Fixtures should label source state as `Source-backed`, `User-provided`, `Inferred from supplied context`, `Data Incomplete`, or `UNKNOWN`.

Do not include live branch names, private repository URLs, real project keys, or internal release names.
