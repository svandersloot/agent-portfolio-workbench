# Sanitized Deployment Runbook Inputs

Store sanitized deployment-note and runbook source excerpts for Release Notes Manager tests.

Use placeholders such as `[deployment notes source]`, `[source-truth branch]`, `[environment]`, `[application]`, and `[approval workflow]`.

Do not include live deployment windows, credentials, production URLs, real environment names, or private operational instructions.
